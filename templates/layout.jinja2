<!DOOCTYPE html>
<html lang="de">
    <head>
        <meta charset="utf-8">
        <link rel="stylesheet" type="text/css" href="{{ url_for('static', filename='style.css') }}">
        <title>{{ title }}</title>
    </head>
    <body>
        <h1>{{ title }}</h1>
        {% if backlink and backlink_caption %}
        <p><a href="{{ backlink }}">{{ backlink_caption }}</a></p>
        {% endif %}
        <div class="content">
        {% block content %}
        {% endblock %}
        </div>
    </body>
</html>
